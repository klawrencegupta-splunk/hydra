apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: splunk-local
  namespace: splunk
spec:
  storageClassName: manual
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 5Gi
