apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: splunk-mds
  namespace: splunk
spec:
  storageClassName: manual
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 10Gi
